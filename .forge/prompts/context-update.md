# Prompt — CONTEXT.md Update

After a session, update `docs/CONTEXT.md` (under 500 tokens).

Include:
- Current phase and status
- What was just done (task IDs)
- What is in progress (max 3)
- Blockers
- Environment state changes
- Next 3 tasks

Do not duplicate `BACKLOG.md`. Link to task IDs only.
